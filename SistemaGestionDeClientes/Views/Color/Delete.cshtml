@model Model.Domain.ControlDeCalzado.Color

@{
    ViewBag.Title = "Color";
}

<h2>Eliminar un Color</h2>

<h3>Estas seguro que quieres eliminar?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DescripcionColor)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DescripcionColor)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar Color" class="btn" /> |
            <a class="btn" href="javascript:void(0);" onclick="history.go(-1);">Volver a la Lista</a>
        </div>
    }
</div>
